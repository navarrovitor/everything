<% if @points.empty? %>
    <div class="container-nothing">
      <h2>Você ainda não avaliou nenhum filme. :/</h2>
      <br>
      <br>
      <p class="text-center"> <%= link_to "AVALIE AGORA ", battlepage_path, class: "btn-purple"  %></p>
    </div>
<% else %>
  <div class="container">
    <h1>Seus filmes preferidos</h1>
    <br>
    <br>
    <% i = 1 %>
    <div class="row">
      <% @points.each do |point| %>
        <div class="col-6 col-lg-3">
          <div class="name">
            <h3><%= i %>. <%= point.movie.title %></h3>
            <h5>(<%= point.movie.year %>)</h5>
            <!-- insert <div class="card"> -->
          </div>
        </div>
        <div class="col-6 col-lg-3">
          <div class="card recommendation">
          </div>
        </div>
        <% i += 1 %>  
      <% end %>
    </div>
  </div>
<% end %>
